import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Rithika V - Cloud & Infrastructure Engineer',
  description: 'Professional portfolio of Rithika V, Cloud & Infrastructure Engineer specializing in MSSQL, Linux/Unix systems, and infrastructure automation.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
